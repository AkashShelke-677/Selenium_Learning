package ex02_Selenium_Basic;

import org.openqa.selenium.firefox.FirefoxDriver;

public class TestSelenium03 {

    public void test_Selenium01(){
         //If it was Selenium version 3 then we have to do this

       // System.getProperties("webdriver.gecko.driver","/path/of_gecko_driver");

       // FirefoxDriver driver =new FirefoxDriver();
       // driver.get("https://google.com");  Setup the Driver(browser)

    }
}
